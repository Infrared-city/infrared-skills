# The Infrared SDK itself.
# The [fast] extra pulls in orjson for ~2x faster tile-result decoding (optional,
# binary-wheel-only). Fall back to plain `infrared-sdk` if your environment
# can't install binary wheels.
infrared-sdk[fast]>=0.4.10

python-dotenv
matplotlib
plotly
numpy
Pillow

# For 10_real_world_map_overlay.ipynb (interactive OSM basemap overlay):
folium

# For the optional webhook_receiver.py only:
flask
