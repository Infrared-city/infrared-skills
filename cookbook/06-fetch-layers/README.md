# 06 — Fetch layers (no simulation)

Fetches buildings, vegetation, and ground materials over a polygon and plots the layers, without running any analysis. Useful for visually inspecting which layers the SDK will inject into a simulation, and for sanity-checking polygon coverage.

**Run:** `python fetch_layers.py`
**Deps:** `infrared-sdk plotly python-dotenv`
